# June AI 伴学系统 — 项目体检报告

**审计日期**：2026-08-07
**审计时点**：完成 v2.0 后 4 次语义提交 + 合并 GitHub 网页端历史、推送至远程之后

## 整体健康度：🟡 良好但有几处要修（评级 B-）

后端 DDD 分层清晰，前端组件化合理，错误处理基本到位，DeepSeek V4 模型迁移方向正确。主要问题集中在三处：鉴权链路一个真 bug、文件功能是半成品、刷新会丢历史会话。均非致命，建议按优先级修复。

---

## ✅ 做得好的地方

- **后端架构规范**：DDD 分层（core / models / repositories / routes / services），26 个 Python 文件共 1510 行，组织清晰
- **前端组件化合理**：20 个源文件共 2831 行，Markdown/KaTeX 渲染链路完整
- **错误处理覆盖较好**：SSE 流有正确的 AbortController 中断逻辑，try/catch 覆盖关键路径
- **敏感信息基本安全**：代码中无硬编码密钥；真实 DeepSeek key 仅存于本地 `.env`（未进版本库、未推送 GitHub）；入库的 `.env.development` / `.env.example` 均为空值模板

---

## 🔴 高优先级（建议尽快修）

### 1. 鉴权链路复制粘贴 bug
- **位置**：`backend/app/core/security.py:102`
- **现状**：`valid_token = settings.JUNE_API_TOKEN or settings.JUNE_API_TOKEN` —— 两侧完全相同
- **影响**：本意应是「用配置的 token，没有则用自动生成的 token」。当前写法让「留空自动生成 token」机制实际不生效，开发模式鉴权行为可能与预期不符
- **修复**：右操作数应改为自动生成的 token 来源（如 `ensure_token()` 或 config 层的兜底逻辑）

### 2. 文件功能是空壳
- **位置**：`backend/app/routes/sessions.py:131-160`
- **现状**：4 个文件接口（`POST/GET /files`、`DELETE /files/{id}`、`GET /files/{id}/content`）全部返回写死的占位数据（`{"id": "placeholder", ...}`、`"File content placeholder"`）
- **影响**：前端已有 `FileItem` 类型和文件面板 UI，但后端接口不可用 → 功能形同虚设
- **决策**：要么补完实现，要么暂时下线前端入口，避免误导用户

---

## 🟡 中优先级（该处理但不紧急）

### 3. 刷新页面丢失全部历史会话
- **现状**：前端 `useJuneStore` 只 POST 创建会话 + 本地内存维护，**未调用后端的 `GET /api/sessions` 列表接口**
- **影响**：用户刷新浏览器后，之前的对话历史全部消失，是明显的体验硬伤
- **修复**：在 store 初始化或应用启动时拉取会话列表

### 4. 会话数据字段不一致
- **位置**：`backend/app/services/session_service.py`
- **现状**：`_session_to_dict` 返回 `createdAt`（驼峰，✓ 与前端一致），但 `get_session` 返回 `created_at`（下划线，✗）且缺少 `model` 字段
- **影响**：前端 `Session` 类型要求 `createdAt` 和 `model`，调用 `get_session` 时字段对不上会导致渲染出错

### 5. 两个孤儿组件（无任何地方引用）
- `frontend/src/components/ui/CodeBlock.tsx` —— 本次 feat(renderer) 提交引入，但 `MarkdownRenderer` 使用了内部 `FencedCodeBlock`，未引用它
- `frontend/src/hooks/useSSE.ts` —— 已被 `services/sseService.ts`（fetch + ReadableStream 方案）取代
- **处理**：可直接删除

### 6. CORS 配置不规范
- **位置**：`backend/app/main.py:92-93`
- **现状**：`allow_origins=["http://localhost:5173", ..., "*"]` 同时含具体域名和通配符，且 `allow_credentials=True`
- **影响**：浏览器规范禁止「`*` + 凭证」组合；开发环境能跑，生产部署带凭证的跨域请求会被拒
- **修复**：去掉通配符 `*`，或改用环境变量按部署环境切换

### 7. 本地 DeepSeek key 明文存储
- **现状**：真实 key（`sk-47287dd3...`）以明文存于 `backend/.env`
- **风险**：未通过 Git 泄露，但本地明文 + 易随打包/截图/分享外泄
- **建议**：到 DeepSeek 控制台轮换新 key；旧 key 字符串从聊天记录/备忘录中清除；确认 GitHub 仓库为 private

---

## 🟢 低优先级（提示）

- `frontend/src/stores/useJuneStore.ts:437` —— `[L2 Debug]` 调试日志前缀遗留
- `backend/app/routes/sessions.py` —— 多处 `request: Request = None` 默认值写法不规范，FastAPI 行为不稳定
- 后端 8 个路由前端从未调用（`GET /api/sessions`、`GET /api/sessions/{id}`、`GET /api/models`、`PUT /api/config/model` 及 4 个文件接口）—— 死路由
- `backend/app/services/deepseek.py:48-55` —— 无 API key 时的「模拟回复」硬编码长字符串，生产模式应改为直接报错
- `backend/app/main.py:206` —— shutdown 阶段 `asyncio.sleep(5)` 无注释说明

---

## 建议的修复顺序

1. **修 `security.py:102` 的 `or` bug** —— 一行代码，恢复鉴权正确性
2. **决策文件功能** —— 补完实现，或暂时摘掉前端入口
3. **补会话列表持久化** —— 调用 `GET /api/sessions`，修复刷新丢历史
4. **统一 `session_service` 字段命名** —— 全部用驼峰 `createdAt`，补 `model` 字段
5. **清理孤儿代码** —— 删 `CodeBlock.tsx`、`useSSE.ts`，清调试日志
6. **轮换 DeepSeek key + 确认仓库可见性**

---

*本报告由代码静态审计生成，未实际运行项目。可运行性方面：后端 Python 语法编译通过、前端无悬挂引用、依赖完整。*
