# June AI 三期迭代方案 · PRD 评审报告

*评审日期：2026-06-18 · 评审对象：june-feature-roadmap.md (+ june-prodoc.md)*

---

## 总体评价

三期方案方向正确，优先级骨架合理——以体验底线（富文本）→ 回答深度（RAG）→ 差异化壁垒（追问树可视化）的递进逻辑推进，每期可独立交付，不互相阻塞。竞品调研扎实（15+ 产品），技术选型有论证。**主要短板在验收标准几乎空白**——每个功能点"做完"的定义模糊，缺乏可测试的完成条件；**其次是部分功能粒度偏粗**，尤其是第三期涉及五个子系统，有进度失控风险。

---

## 一、需求清晰度

### ✅ 亮点
- **用户画像隐式但可推断**：从"伴学"定位、LaTeX 数学公式支持、追问链机制，能清晰推断目标用户为深度学习场景的学生和研究人员，不是泛聊天用户
- **核心场景有完整链路**：选中文字追问 → 悬浮窗打开 → 父线程上下文注入 → 多层嵌套追问 → 知识树形成，这条闭环是清晰的
- **竞品差异点定位准确**：方案明确指出 June 在"追问树可视化 + 本地优先 RAG"上形成差异，不是盲目跟风竞品功能清单
- **每期都有"为什么优先"的论证**：一期是体验底线，二期是回答质量跃迁，三期是差异化能力——这个逻辑链在方案中表达充分

### ⚠️ 问题
- **缺少量化用户故事**：没有"作为 XX 用户，我想要 XX，以便 XX"的结构化描述，导致某些功能点的设计动机不够明确。例如"收藏与标签"是为复习场景还是为知识管理场景服务？两个场景的产品形态完全不同
- **"追问树大小"的假设未明确**：方案提到"Node 数通常不超过 50 个"，这是基于什么场景的假设？一次考试复习可能横跨多个章节，追问树可能远超 50 个节点。如果在需求阶段不做明确假设，后续交互设计和性能优化会反复
- **第二期的"严格 KB 模式"边界模糊**：方案提及竞品 GangDan 的"只从资料库回答"模式，但没说明 June 是否要支持类似模式。这会影响 RAG 注入策略的设计——是"增强模式"（资料 + 模型知识混合）还是"严格模式"（仅资料内容）

### 💡 建议
- 为每期写 2-3 个量化用户故事，例如：
  > "作为复习机器学习课程的学生，当我上传《西瓜书》PDF 后追问'信息增益的公式推导'，AI 能引用书中原文段落并标注页码，回答准确率 ≥ 80%"
- 明确声明追问树的规模假设：主对话节点 + L2 追问 ≤ 30 个、总节点（含 L3/L4）≤ 200 个。这是第一版的硬边界，超出后滚动加载或折叠
- 第三期的"对话导出"场景需拆开：Markdown 导出服务于笔记沉淀（学生 → Notion/Obsidian），JSON 导出服务于跨设备恢复（开发者/重度用户），设计动机和优先级不同

---

## 二、边界范围

### ✅ 亮点
- **三期均有明确的"本期不做什么"隐含边界**：每期只列了强相关的功能点，没有把竞品的所有功能塞进一期
- **技术选型有备选方案**：sqlite-vec → ChromaDB 的回退路径；自绘 SVG → ReactFlow 的升级路径。这给实施留了弹性
- **"后端无需改动（一期）"是聪明的范围控制**：一期只动前端，风险低、回归测试少、发布快——这是正确的工程决策
- **每期独立可交付的设计是亮点**：用户可以在第一期就获得可读的 Markdown 回复，不必等三期全部完成

### ⚠️ 问题
- **第一期"快捷操作浮层"有范围蔓延风险**：消息气泡 hover 操作栏中的"重新生成"和"反馈（👍👎）"看起来轻量，但涉及后端 SSE 中断与重发、反馈数据存储与统计，实际上会拖慢第一期交付。建议第一期只做"复制全文"，其他操作推至后续
- **第二期缺少"RAG 结果为空"的降级策略**：当检索结果为空或相关性低时，AI 应该怎么回答？是退回通用知识、明确告知"资料库中未找到"、还是混合回答？这个边界条件影响整体体验，需要在需求阶段明确
- **第三期范围过大**：五个子系统（可视化面板、对话搜索、收藏标签、多格式导出、会话恢复）的总估时 12-18 天过于乐观。仅"会话恢复"一项——还原悬浮窗位置 + 窗口状态 + 追问上下文——就涉及复杂的状态序列化和反序列化，可能在 Window Manager 层有大量边界情况
- **跨期功能遗漏：一期改进后对二期的影响**：一期引入 Markdown 渲染后，二期做文档解析时需要去掉 Markdown 语法再分块（否则 chunk 中混入 `**` `#` 等标记污染向量质量），这个跨期关联没有提及

### 💡 建议
- **第一期缩减范围**：只做 Markdown 流式渲染引擎 + 代码块增强（高亮 + 复制按钮）+ LaTeX + 表格。移除快捷操作浮层、移除引用来源标注角标、移除图片 lightbox。这些移到第一期之后的一个快速迭代（1.1 版本）
- **第二期增加 RAG 模式开关**：在追问设置面板新增一个 toggle——"仅从资料库回答 / 混合回答"。默认"混合"，满足 80% 场景；需要严谨学术引用时切"仅资料库"
- **第三期拆分**：建议拆为 3A（追问树可视化 + 会话恢复，核心差异化）和 3B（搜索 + 收藏标签 + 导出，辅助生产力）。3A 先交付，3B 紧跟。这样即使 3B 延期，核心差异化功能已上线
- **增加"不做什么"清单**：明确声明每期不做的功能。例如：
  - 第一期不做：代码运行、消息编辑、语音朗读
  - 第二期不做：视频/音频文件解析、OCR 图片文字提取、多资料库
  - 第三期不做：协作共享追问树、公开资料库市场

---

## 三、验收标准

### ✅ 亮点
- **估时有参考价值**：每期给出了天数范围，说明有一定的工作量拆解
- **技术方案有具体指标**：如流式缓冲 300ms、chunk ~500 tokens、向量检索 top-10 + BM25 top-5，这些数值给实施提供了明确锚点

### ⚠️ 问题（**核心缺陷**）
验收标准是三期方案最严重的缺失。整个 roadmap 描述了"做什么"和"怎么实现"，但几乎没有"怎样算完成"的可测试条件。

具体缺失包括：

| 缺失领域 | 需要但未提供的验收标准示例 |
|---------|------------------------|
| 渲染性能 | 一期：流式渲染 2000 字 Markdown（含代码块 + 公式），首帧渲染 ≤ 50ms，后续帧 ≤ 16ms，CPU 占用 ≤ 30%。追问窗切换时不闪烁 |
| LaTeX 正确性 | 一期：支持 LaTeX 的哪些子集？`\begin{align}`, `\begin{cases}`, `\xrightarrow` 是否覆盖？渲染失败时如何降级？ |
| 文档解析质量 | 二期：PDF 解析文本保留率 ≥ 95%（含中英文混排）、公式区域标记为特殊 block（不暴力分行）、表格保留结构化 |
| 检索准确率 | 二期：以 10 份标准学习文档构建测试集，Top-3 召回率 ≥ 80%，MRR ≥ 0.75。缺少这个量化指标，RAG 管线调优没有方向 |
| 追问树交互 | 三期：50 节点树首次渲染 ≤ 200ms，点击节点到悬浮窗前置 ≤ 100ms。画布平移≥ 30fps，缩放不触发节点重排 |
| 导出完整性 | 三期：Markdown 导出的追问层级缩进与原始对话结构一致，LaTeX 和代码块保留原始格式，导出文件可被 Obsidian/Typora 正常渲染 |
| 会话恢复 | 三期：恢复成功率 ≥ 99%，悬浮窗位置偏移 ≤ 5px（屏幕坐标），窗口 zIndex 层级恢复正确 |
| 跨期兼容 | 一期升级后，已有的 v2.0 会话消息渲染不报错（向后兼容）。SQLite 中 `content` 字段存的是 Markdown 原文，前端渲染即可 |

### 💡 建议
- **每期至少定义 5 个量化验收条件**，覆盖：性能指标、正确性指标、降级行为、向后兼容
- **第一期的 LaTeX 子集测试清单**是最高优先级的验收项——学术场景中 LaTeX 公式出错比纯文本更糟（误导性），建议先收集 20 个高频 LaTeX 表达式（来自机器学习/数学教材）做渲染对照测试
- **第二期 RAG 的准确率测试必须在正式开发前搭建**：准备 5-10 份标准学习文档 + 每个文档 5 个事实性问题，形成 ground-truth 评测集。Pipeline 每改一次就跑一次评测
- **第三期的会话恢复需要单独的测试套件**：自动创建带 10 个悬浮窗、不同位置和层级的会话，关闭后恢复，比对每个窗口的状态。这是回归测试重点

---

## 四、风险依赖

### ✅ 亮点
- **技术风险识别务实**：sqlite-vec Windows 编译问题、react-markdown 与 Tailwind CSS 4 的样式覆盖——这些都是实际开发中会踩的坑，提前识别并给出备选方案
- **进程守护已有基础设施**：watchdog.py 的存在降低了 RAG 后端服务的稳定性风险
- **流式渲染性能风险有应对方案**：300ms 缓冲 + requestAnimationFrame 批量帧策略有竞品验证

### ⚠️ 问题
- **缺少 SSE 与 Markdown 流式渲染的兼容性分析**：DeepSeek API 的 SSE delta 是按 token 粒度推送的。Markdown 语法元素（如 ` ```python `、`$$...$$`）可能跨多个 delta 拆分到达，导致前端在拼接时出现短暂的不完整语法块（例如先收到 ` ```py`，下一个 delta 才收到 `thon`）。300ms 缓冲不能解决这个问题——需要在 Markdown 解析层做「未闭合语法块的优雅降级渲染」
- **缺少多悬浮窗 + Markdown 渲染的内存评估**：当前 v2.0 悬浮窗用纯文本，内存压力小。引入 react-markdown + KaTeX 后，每个悬浮窗都可能持有独立的渲染树。如果用户同时打开 10 个 L3 追问窗，每个窗口内有公式 + 代码块，内存占用可能从 MB 级跳至数十 MB。方案中没有提到悬浮窗的虚拟化或懒渲染策略
- **第二期的 pydantic-core / PyMuPDF 编译依赖链**：文档提到 setup.bat 已解决 pydantic-core 编译问题，但 PyMuPDF (fitz) 在 Windows 上同样需要预编译 wheel 或 C++ 编译环境。如果用户没有 Visual Studio Build Tools，安装会失败。需要提前验证
- **第三期的 Dagre 布局与追问树语义的匹配度未验证**：Dagre 处理的是 DAG（有向无环图）自动布局，但追问树本质上是**严格树结构**（每个追问只有一个父节点），用 DAG 布局引擎可能产生不必要的视觉复杂度（如分支线交叉）。如果只是树，用简单的递归布局算法（纵轴 = 层级，横轴 = 兄弟顺序）就能解决问题，不需要引入 Dagre
- **竞品快速迭代的竞争风险**：方案中提到 ChatTree、Prompt Tree 等竞品已具备分支可视化，但未评估它们在 3-4 个月内可能的产品演进。June 三期总耗时 9 周，期间竞品可能在可视化能力和导出格式上进一步领先
- **缺少 API Key 安全风险在 RAG 场景下的重新评估**：当前 API Key 通过 `PUT /api/config/api-key` 设置。第二期 RAG 引入后，外部网页 URL 抓取环节（后端 fetch + readability）存在 SSRF 风险——如果用户提交恶意 URL，后端可能在用户不知情的情况下请求内网地址

### 💡 建议
- **第一期增加 SSE 分片合并测试**：构造一个 Markdown 内容列表（含代码块、LaTeX 块、嵌套列表），用脚本模拟 SSE delta 的随机拆分（每 1-N 个 token 发一个 delta），验证前端渲染无残缺语法块。这是第一期最容易被忽视的 bug 来源
- **第二期启动前做 pypdfium2 替代评估**：PyMuPDF 的 C 扩展依赖是已知痛点。纯 Python 的 `pypdfium2`（PDFium 的 Python 绑定，wheel 更成熟）或 `pdfplumber` 可以作为备选，降低 Windows 安装门槛
- **第二期增加 SSRF 防护**：URL 抓取功能需要加入请求白名单/黑名单检查、内网 IP 过滤、超时限制（≤10s）、文件大小上限（≤20MB），这些安全措施应明确写入方案
- **第三期的可视化方案应先做技术验证（PoC）**：在正式启动第三期前，用最小代码实现一个 30 节点的追问树 SVG 渲染 + Dagre 布局原型，验证布局效果和性能。一个下午的 PoC 可以避免 5 天开发后推倒重来
- **考虑缩短整体周期**：三期 9 周的品牌窗口期可能过长。如果资源允许，建议将第一期压缩到 1 周（只做渲染核心，去掉操作浮层），第二期压缩到 2 周（先做最小 RAG——只支持 PDF + Markdown，纯关键词检索 + 简单注入），集中兵力在 3-4 周内冲刺第三期的可视化差异化能力

---

## 五、优先级调整建议

### 当前优先级

```
第一期(2周)          第二期(3周)          第三期(4周)
富文本渲染     →    本地知识库 RAG    →    对话管理与可视化
```

方向正确，但内部功能点的排序和粒度需要微调。

### 建议调整后的优先级

```
第一期·精简版(1-1.5周)    第二期·最小RAG(2周)      第三期A(2.5周)         第三期B(2周)
├─ Markdown 流式渲染      ├─ PDF/MD 解析+分块     ├─ 追问树可视化面板     ├─ 对话内搜索
├─ 代码高亮 + 复制        ├─ 关键词检索(BM25)     ├─ 会话恢复与历史       ├─ 收藏与标签
├─ LaTeX 数学渲染         ├─ Prompt 注入          ├─ Markdown 导出        ├─ 多格式导出(PDF/JSON)
├─ GFM 表格               ├─ 前端上传+资料面板     │                       └─ 搜索与标签联动
└─ SSE 分片兼容处理        └─ 混合检索模式开关      └─ [独立可交付]
   [独立可交付]
                          [独立可交付]
                          
   🚫 本期不做：               🚫 本期不做：               🚫 本期不做：
   操作浮层/反馈              向量检索/rerank             标签自动建议
   引用角标                   多格式文档(网页URL)          树结构编辑/拖拽
   图片lightbox               ChromaDB集成               PNG/SVG导出
   代码运行按钮               RAG评测体系                 MindMap模式
```

### 调整理由

1. **第一期精简的理由**：富文本渲染本身已经是一个不小的前端工程——Markdown 流式解析 + LaTeX + 代码高亮 + 表格，足以发一个独立版本。操作浮层、引用角标、图片 lightbox 都是锦上添花，不应阻塞核心渲染的发版节奏
2. **第二期拆"两步走"的原因**：完整的 Hybrid RAG（向量 + BM25 + rerank + embedding 模型）是典型的"第二系统效应"——想要做全但容易延期。先做纯关键词检索（BM25），验证文档解析 → 分块 → 检索 → 注入的完整链路跑通，用户就能用上了。向量化和 rerank 在后续迭代中渐进加入
3. **第三期拆 A/B 的原因**：追问树可视化是 June 最独特的能力，应该在第三期中最先交付，能用的越早越好。搜索、收藏、标签是标准的"效率工具"功能，独立性强，可以并行或在 A 之后交付
4. **会话恢复提到第三期 A**：会话恢复与追问树可视化共享数据结构（树节点 = 会话恢复单元），放在同一期开发更高效

### 跨期交叉项（建议）

| 交叉项 | 内容 | 建议 |
|-------|------|------|
| 一期 → 二期 | Markdown 格式对文档分块质量的影响 | 文档解析时先 strip Markdown 语法再分块，或保留结构化标记用于 chunk metadata |
| 一期 → 三期 | 追问树的节点内容显示依赖 Markdown 渲染 | 三期追问树节点预览需要复用一期的 MarkdownRenderer 组件（截断显示） |
| 二期 → 三期 | RAG 检索结果需要在追问树中可视化 | 三期追问树节点可显示"该回答引用了 N 条资料"，来自二期的引用元数据 |
| 全局 | Tailwind CSS 4 样式体系兼容性 | 一期引入的 prose 类可能与后续组件的样式冲突，建议在 `tailwind.config` 中统一管理 typography plugin 配置 |

---

## 六、优先级行动建议

### [本周]
1. **进行 SSE 分片渲染 PoC**：写一个最小 demo，模拟 DeepSeek SSE 返回含 LaTeX 块和代码块的 Markdown，按 token 随机拆分 delta，验证 react-markdown + KaTeX 的流式渲染无残缺语法块
2. **收集 LaTeX 验收测试用例**：从常见机器学习/数学教材中提取 20 个高频 LaTeX 表达式（行列式、求和、积分、矩阵、分段函数、希腊字母），作为一期渲染的对照测试集
3. **确认 sqlite-vec Windows 编译可行性**：在干净的 Windows 环境中 `pip install sqlite-vec`，验证是否需要 Visual Studio Build Tools。如果失败，直接确认用 ChromaDB 作为备选

### [本月]
4. **搭建 RAG 评测基础设施**：准备 5 份标准学习文档 + 每份 5 个事实性问题，形成 ground-truth QA 对，作为第二期 Pipeline 开发的评测基线
5. **启动追问树可视化 PoC**：用 Dagre + SVG 或自绘递归布局渲染一个 30 节点的追问树，验证布局效果和 200ms 渲染目标可达性
6. **编写每期的量化验收清单**：参照本报告第四节中缺失的验收标准，为每期补充 5+ 个可测试的完成条件

### [发布前]
7. **一期上线前做 10 条 v2.0 会话的向后兼容测试**：确保旧会话中的纯文本消息在升级后能正常显示（不报错、不丢失内容）
8. **二期的 SSRF 安全审查**：URL 抓取功能上线前做一轮安全测试（内网地址、超长 URL、恶意重定向）
9. **三期的会话恢复回归测试套件**：自动化创建复杂会话状态 → 关闭 → 恢复 → 比对差异的 E2E 测试

---

*本评审基于 june-prodoc.md (v2.0 产品现状) + june-feature-roadmap.md (三期迭代方案) 两份文档。建议在三期正式开工前，按本报告的优先级调整建议更新 roadmap，并补充验收标准清单。*
