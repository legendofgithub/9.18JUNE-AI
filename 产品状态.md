# 产品状态

> 交接目标：本文档面向下一个接手的 AI 工具或开发者。读取本文档后，应立即理解项目经历过哪些关键转型、当前处于什么阶段、哪些能力已经可用、哪些内容是临时测试态，以及上线前不能遗漏的边界。

## 1. 状态摘要

| 字段 | 当前状态 |
| --- | --- |
| 产品名称 | `June AI assistant` |
| 当前定位 | 用 Vibe Coding / 自然语言指挥 AI，做出可试用、可售卖、可交付的最小商业 MVP |
| 项目阶段 | 本地可测试的商业化 MVP，不是生产可上线版本 |
| 当前核心流程 | 首页一次性购买解锁 → 进入模型服务 → 启动超级个体训练师人格 → 自动装载 skill 并进入 10 节点交付流程 |
| 登录状态 | 已启用登录门禁；未登录只能看到登录/注册页；初始管理员账号为 Tony |
| 支付状态 | 沙箱支付，未接真实微信支付或支付宝 |
| 本地测试数据 | 原测试访客数据仍在开发库中；Tony 管理员账号可免真实支付完成解锁 |
| 最后更新 | 2026-08-17 |

## 2. 产品演变链路

项目经历了四次定位变化，后续接手时不要用早期定位理解当前产品。

1. **AI 扫盲学堂**
   - 最初目标是做 AI 技术和框架启蒙。
   - 后来被明确放弃：用户不需要学技术，也不看代码。

2. **超级个体训练官**
   - 转型为 AI 变现导向的服务型 MVP 训练。
   - 建立了商业链路：商品、订单、解锁、训练师人格、流程节点、交付物、AI 跟踪和完成锁定。

3. **Vibe Coding 变现训练官**
   - 产品主线从「AI 服务 MVP」升级为「用自然语言做出最小商业 MVP」。
   - 用户侧全面去技术化，不教编程语言、框架、数据库、接口、部署架构或代码结构。
   - 只保留少量必要应用概念：页面、按钮、表单、上传、生成结果、复制、下载、链接、收款、交付、验收。

4. **June AI assistant 登录门禁态**
   - 前端展示品牌改为 `June AI assistant`。
   - 删除首页免费试讲模块。
   - 恢复显式登录/注册表单。
   - 删除自动创建访客身份逻辑。
   - 产品首页和模型服务页解耦，分别使用 `#/` 与 `#/studio`。
   - 未登录用户不能看到订单、解锁状态、模型服务和工作台数据。

## 3. 当前商业模式

### 商品

| 商品 | 价格 | 权益 |
| --- | ---: | --- |
| 超级个体训练师解锁 | 39 元 | 一次性获得在本页启动超级个体训练师人格的权限 |

商品种子数据位于：

- `backend/app/repositories/commerce_repo.py`
- `PRODUCTS`

### 训练师启动规则

- 是否可启动只看当前账号是否存在已支付订单。
- 未付费不能启动超级个体训练师人格。
- 首次启动必须填写模型访问密钥；密钥验证通过后加密保存。
- 后续启动自动复用已保存密钥，不需要重复填写。
- 10 个必修节点必须按顺序完成。
- 每个完成节点的交付物至少 20 个字符。
- 全部节点客观完成后，`mvp_runs.status = completed`。
- 完成后只允许查看归档内容和报告。
- 主对话、追问、节点修改、重新启用、补课都必须拒绝 completed 路径。
- 用户口头说「我还没完成产品打造」不能改变客观完成状态。

### 承诺边界

产品不承诺收入，只承诺确定性交付物：

- 付费人群与痛点画布
- 可售卖结果与验收标准
- Vibe Coding 商业需求简报
- 最小产品形态选择说明
- 第一版 MVP 指令与试用说明
- 5 分钟迭代记录
- 报价与收款说明
- 获客素材与潜在客户名单
- 首次销售与交付记录
- 变现复盘报告

## 4. 当前 10 节训练路径

课程种子数据位于：

- `backend/app/repositories/commerce_repo.py`
- `MVP_STEPS`

节点顺序：

1. 选择愿意付费的人群和痛点。
2. 定义一个最小可售卖结果。
3. 写出 Vibe Coding 商业需求简报。
4. 选择最小产品形态。
5. 用自然语言让 AI 生成第一版 MVP。
6. 用 5 分钟迭代法改到可试用。
7. 制作报价、收款方式和交付说明。
8. 准备获客素材和首批 20 个潜在客户。
9. 发起首次销售并完成一次最小交付。
10. 复盘转化、交付和下一轮迭代。

重要边界：

- 已存在的进行中路径不强制迁移。
- 新创建的路径才使用新版 Vibe Coding 课程。
- 不要为了统一数据而重写历史路径。

## 5. 核心技术结构

### 后端核心文件

| 文件 | 职责 |
| --- | --- |
| `backend/app/repositories/commerce_repo.py` | 商品、订单、已支付解锁、训练师安装状态、模型服务目录、流程节点、交付物、事件持久化 |
| `backend/app/services/commerce_service.py` | 支付确认、训练师状态、模型服务目录、AI 工具连接、启动超级个体训练师 |
| `backend/app/services/mvp_service.py` | 路径详情、节点顺序、主对话、追问链、AI 跟踪、报告 |
| `backend/app/services/auth_service.py` | 注册、登录、用户 Token |
| `backend/app/routes/auth.py` | 用户认证 API |
| `backend/app/routes/commerce.py` | 商品、订单、训练师状态与启动、模型服务目录、流程、对话、报告 API |

### 前端核心文件

| 文件 | 职责 |
| --- |
| `frontend/src/components/commerce/CommerceApp.tsx` | 产品外壳、hash 路由、登录门禁、首页/模型服务切换 |
| `frontend/src/components/commerce/SiteHeader.tsx` | logo、产品介绍、关于我们、语言切换、头像悬浮窗 |
| `frontend/src/components/commerce/LandingView.tsx` | 产品宣传页、10 节点介绍、套餐购买、支付确认、登录/注册 |
| `frontend/src/components/commerce/WorkspaceView.tsx` | 浅色（白底）三栏模型服务页、流程列表、节点交付物、主对话、追问链、报告下载 |
| `frontend/src/components/commerce/ModelServicesPanel.tsx` | 模型服务总览、供应商编辑卡、模型清单、服务端探测、草稿应用 |
| `frontend/src/stores/useCommerceStore.ts` | 商业流程状态管理、登录会话、订单历史、API 请求、SSE |
| `frontend/src/types/index.ts` | AI 工具显示映射和商业数据类型 |

### 数据模型

核心表：

- `products`
- `orders`
- `entitlements`
- `installed_skills`
- `model_services`
- `model_entries`
- `mvp_runs`
- `run_steps`
- `run_artifacts`
- `run_events`

### 关键 API

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/products`
- `POST /api/orders`
- `POST /api/orders/{id}/confirm`
- `GET /api/orders`
- `GET /api/coach/status`
- `POST /api/coach/start`
- `GET /api/model-services`
- `POST /api/model-services`
- `PUT /api/model-services/{id}`
- `DELETE /api/model-services/{id}`
- `POST /api/model-services/{id}/discover`
- `POST /api/model-services/{id}/activate/{model_id}`
- `GET /api/skills/current`
- `POST /api/skills/install`
- `GET /api/mvp-runs`
- `POST /api/mvp-runs`
- `GET /api/mvp-runs/{id}`
- `PATCH /api/mvp-runs/{id}/steps/{step_id}`
- `POST /api/mvp-runs/{id}/chat`
- `POST /api/mvp-runs/{id}/follow-up`
- `GET /api/mvp-runs/{id}/report`

## 6. 登录与账号体系

这是当前最容易误判的部分，接手时必须特别注意。

### 当前实现

- 前端已恢复登录/注册页，未登录只能看到账号登录界面。
- 前端已拆成两个页面：`#/` 产品首页和 `#/studio` 模型服务页。
- 首页包含产品介绍、关于我们、套餐购买、登录/注册和支付确认。
- 模型服务页包含“启动超级个体训练师人格”、AI 工具选择、访问密钥连接、节点、主对话和追问链。
- 模型服务页采用浅色（白底）三栏桌面布局：左侧会话与流程导航、中间模型服务总览、右侧详情与对话。
- 左栏支持 264-420px 拖拽与 56px 折叠；右栏支持 300-520px 拖拽与关闭；窄屏自动折叠/关闭并保证主栏占满。
- 模型服务行显示厂商、协议、模型数和 Key 状态；展开后可编辑只写 API Key、端点、显示名、协议与模型清单。
- 模型编辑支持上下文 `K/M` 人性化输入、服务端模型探测、草稿独立保存、取消/应用、版本冲突检测和删除确认。
- Composer 内提供两级供应商/模型选择器，并显示推理强度档位。
- 点击首页“进入模型服务”会播放左右科技铁门合拢、碰撞震动和灰尘扩散；遮罩消失后直接呈现模型服务，没有开门动画。
- 顶部导航左侧为 logo 和产品名，右侧为语言切换和用户头像。
- 头像悬浮窗展示账号、邮箱、密码加密状态和最近购买历史。
- 未登录访问 `#/studio` 会回到首页。
- 未登录只能看到官网营销内容和公开商品列表。
- 订单、解锁状态、安装状态、模型服务和工作台数据仍必须登录后访问。
- 前端 `useCommerceStore.bootstrap()` 只读取本地 Token 并调用 `/api/auth/me` 恢复会话。
- 不再自动创建 `guest-...@june.local` 访客身份。
- 退出登录会清理本地 Token 和当前商业状态。

### 管理员账号

- 初始账号：`Tony`
- 管理员角色存在 `users.is_admin`。
- 管理员账号由启动流程从 `JUNE_ADMIN_IDENTITY`、`JUNE_ADMIN_EMAIL`、`JUNE_ADMIN_DISPLAY_NAME`、`JUNE_ADMIN_PASSWORD` 初始化。
- 管理员密码只配置在本地 `backend/.env` 或部署环境变量中，不写入源码和 `.env.example`。
- 管理员点击购买后，订单直接置为已支付并解锁训练师启动权限，不进入真实支付或沙箱确认。
- 管理员和普通用户都会进入同一个模型服务页；API 连接设置只出现在模型服务页。

### 仍待完成

1. 已有访客测试数据如何迁移、隔离或删除。
2. 登录密码找回、登录限流、账号禁用和生产部署账号治理。
3. 管理员操作审计。

### 本地测试数据注意事项

- 2026-08-16 出现过一次测试访客拆分：两笔沙箱已支付订单共 3 条路径在数据库中归属其中一个访客，浏览器页面实际使用另一个访客，导致「连接并启动」因 `availablePaths = 0` 被禁用。
- 已通过本地数据库修复，将两笔已支付订单和 3 条可用路径迁回浏览器当前使用的测试访客。
- 修复前已备份数据库：`backend/june-before-guest-repair-20260816-152111.db`。
- 这只是本地测试数据修复，不代表正式上线后的订单迁移方案。

## 7. BYOK / 连接 AI 工具

### 用户侧表现

- 页面文案叫「连接 AI 工具」。
- 用户只看到：
  - 选择 AI 工具
  - 访问密钥
- 不显示 Base URL、模型调用、服务端代理等技术细节。

### 实际机制

- `AI_TOOL_OPTIONS` 提供用户友好的工具名称。
- 前端根据所选工具自动匹配内部 `MODEL_BASE_URLS`。
- 请求仍会发送 `base_url` 给后端。
- 后端做连接测试。
- 连接通过后写入 `installed_skills`。
- 访问密钥加密存储，API 不回传明文。

### 费用边界

- AI 使用费用由用户自己的 AI 工具账户承担。
- 平台不承担模型推理费用。

### 按钮状态规则

「启动超级个体训练师人格」只有在以下条件全部满足时才可点击：

1. 当前用户已登录。
2. 当前账号存在已支付订单。
3. 首次启动时访问密钥输入框非空，或已有加密保存的访问密钥。
4. 当前没有启动请求正在处理。

未填写访问密钥时可以点击启动按钮，系统会展开并聚焦密钥输入框。已保存密钥时无需重复填写；未付费时按钮禁用并引导回首页购买。

### 连接失败诊断

2026-08-16 用户使用 DeepSeek 访问密钥连接失败。排查确认：

- 本机可以访问 DeepSeek 的 `/v1/chat/completions` 端点。
- 使用假密钥会收到 DeepSeek 标准 401 认证失败，说明端点和请求格式兼容。
- 原实现把服务商真实错误替换成统一的「连接 AI 工具失败」，导致无法区分密钥无效、余额不足、权限不足、限流或服务波动。

已改造：

- `DeepSeekService.test_connection()` 对 401、402、403、404、408、429、5xx 和网络异常做用户可读分类。
- 错误信息不回显访问密钥，也不返回服务商原始响应。
- `CommerceService.start_coach()` 将分类后的原因传给前端。
- 新增测试确认失败原因会返回、访问密钥不会泄露。

## 8. AI 训练官行为规则

提示词和工具模板位于：

- `backend/app/services/mvp_service.py`
- `STEP_TOOLS`
- `_build_messages()`
- `_build_follow_up_messages()`

### 必须遵守

- 用户是零基础商业者，不是开发者。
- 不主动讲解技术原理。
- 不要求用户查看或修改代码。
- 每次回答必须推进当前商业节点。
- 只围绕商业目标、用户反馈、可用性、售价、获客和交付。
- 遇到技术问题，转译为用户想要的效果：
  - 用户看到什么
  - 用户点什么
  - 用户填写什么
  - 用户得到什么
  - 怎么收款
- 主对话末尾输出服务端专用 `<tracking>` 元数据。
- 前端和 API 响应不能向用户暴露 `<tracking>` 内容。

### 禁止内容

不要在用户侧主流程主动出现：

- API
- Base URL
- 模型调用
- 框架
- 代码结构
- Prompt 工程
- CORS
- 数据库
- 接口
- 部署架构

内部工程文档和测试代码可以讨论这些概念，但用户主流程不能。

## 9. 支付状态

当前是沙箱支付：

- 前端可生成沙箱流水号。
- 用户点击确认到账后，后端写入订单支付状态。
- 支付成功后解锁训练师启动权限。
- 同流水号重复确认保持幂等。
- 不同流水号重复确认会拒绝。

前端购买流程（2026-08-17 实现）：

- `#/product` 点击「立即购买」→ 调用 `createOrder` 创建订单并跳转 `#/payment`；未登录先跳 `#auth`，已有 pending/paid 订单则直接进入对应页面。
- `#/payment` 为付款页（替换原空白黑页），展示商户单号、商品、金额；点击「付款」以沙箱流水号 `SANDBOX-{providerOrderId}` 调用 `POST /api/orders/{id}/confirm` 模拟现金到账信号。
- 后端确认已支付后跳转 `#/success`（购买成功页），页面居中展示「进入模型服务」按钮跳转 `#/studio`。
- 跳转由**后端订单已支付状态**（`coachStatus.paid` / `order.status==='paid'`）驱动，不以前端按钮硬跳。
- 接入真实支付时，只需让支付渠道回调同一 `POST /api/orders/{id}/confirm` 端点（生产模式带 `signature` 验签），付款页与成功页逻辑完全复用，无需改动。

尚未完成：

- 微信支付
- 支付宝
- 商户资质
- 真实回调验签
- 退款
- 对账
- 发票

生产模式目前会 fail closed，不能使用 sandbox 支付直接上线。

## 10. 防导出边界

目标：

- 付费课程内容只在服务端保存。
- API 只返回当前节点渲染所需内容。
- 不提供课程 Markdown 源文件导出。
- 不提供课程 JSON 导出。
- 不提供 Prompt 包导出。
- 不提供批量复制接口。

边界：

- 无法阻止截图。
- 无法阻止人工抄录。

不要向用户夸大保护能力。

## 11. 已验证能力

最近一轮完整验证结果：

### 后端

```text
102 passed（以实跑为准：pytest tests -q --ignore=tests/test_desktop_runtime.py）
```

覆盖：

- 单一解锁商品定价
- 支付幂等
- 已支付订单解锁
- 未支付不能启动
- 启动后自动装载 skill 并创建流程
- 已保存密钥可复用启动
- 重复启动不重复创建流程
- 新版 10 节课程
- 节点顺序
- 交付物长度
- 完成锁定
- 主对话锁定
- 追问锁定
- 节点修改锁定
- 权限隔离
- BYOK 调用
- AI 跟踪元数据隐藏
- 订单历史
- 当前模型连接状态

### 前端

```text
2 passed（仅 `sseService`、`harnessRestore` 两个用例，商业核心流程无前端测试覆盖）
build 成功
```

### 浏览器 E2E

已通过：

1. 用户进入首页并登录。
2. 看到唯一 39 元解锁商品。
3. 管理员点击购买后直接解锁。
4. 点击“进入模型服务”触发铁门转场。
5. 点击“启动超级个体训练师人格”。
6. 未填写密钥时自动聚焦访问密钥输入框。
7. 填写密钥后启动并进入主对话。
8. AI 跟踪更新阻塞点和下一步。
9. 完成 10 个节点后流程锁定。

### 布局

已检查：

- 桌面视口无横向溢出
- `390×844` 移动端无横向溢出
- 无页面脚本错误

最近一次 UI 验证还确认：

- 首页和模型服务页已解耦，路由分别为 `#/` 与 `#/studio`
- 首页包含产品介绍、关于我们、一次性解锁商品、登录/注册与支付确认
- 语言切换可在中文/英文首页文案间切换
- 头像悬浮窗能展示账号、密码加密状态与购买历史
- API 连接设置只出现在模型服务页
- 点击“进入模型服务”立即出现铁门转场，结束后无残留遮罩
- `390×844` 移动端无横向溢出

## 12. 当前已知不一致

### README

`README.md` 已同步产品首页 `#/` 和模型服务页 `#/studio` 的使用流程。

### COMMERCIALIZATION.md

`COMMERCIALIZATION.md` 已更新为当前产品视角的产品文档。

内容覆盖当前用户画像、用户需求、产品旅程、商业规则、关键指标、上线优先级和后续产品细节。

旧的伴学/教育订阅路线已被替换，不要按旧内容实施。

### audit-2026-08-07.md

这是旧项目体检报告，反映的是商业化改造前的技术状态。

其中部分问题可能已经被后续改动解决，不能直接当作当前缺陷清单。

### june-iteration-prd-review.md

这是旧 PRD 评审文档，主要围绕学习功能、追问树、RAG 和报告。

不能代表当前 Vibe Coding 变现产品需求。

### 前端遗留 harness 聊天 UI（已清理）

旧版 `useJuneStore`、`src/components/chat|float|menu/`、`WelcomeOverlay` 等遗留子系统已在 8.30 快照前删除，当前 `src/` 只包含商业产品代码（`stores/commerce/` 切片、`useHarnessStore`、`components/commerce/`）。

2026-09-18 可维护性重构进一步：网络层统一到 `services/apiClient.ts`，类型拆分 `types/commerce.ts` / `types/harness.ts`（清除 18 个死类型），`useCommerceStore` 按域切片组合（对外 API 不变），`ProjectWorkspace` 拆出 `workspace/` 子组件。

## 13. 上线前待办

按优先级排列：

1. **真实支付**
   - 准备微信支付或支付宝商户资质。
   - 接正式下单、回调、验签、退款、对账。

2. **生产安全配置**
   - `JUNE_AUTH_SECRET`
   - `JUNE_PAYMENT_CALLBACK_SECRET`
   - `JUNE_API_TOKEN`
   - 生产环境 fail closed 检查

3. **部署方案**
   - 域名
   - HTTPS
   - 数据库
   - 备份
   - 日志
   - 监控

4. **用户协议与合规文案**
   - 不承诺收入
   - 虚拟商品边界
   - 退款规则
   - 付费解锁边界
   - AI 生成内容声明

5. **测试数据治理**
   - 清理本地访客测试数据。
   - 区分开发库和生产库。
   - 禁止把真实用户密钥写入日志。

6. **最小运营后台**
   - 查询订单
   - 查询用户解锁状态
   - 处理异常支付
   - 处理退款

## 14. 接手警告

1. 当前 git 工作区有大量未提交改动。
2. 多个核心商业化文件仍是未跟踪状态。
3. 不要执行 `git reset --hard`。
4. 不要用 `git checkout --` 回滚用户改动。
5. 不要把旧教育产品文档当作当前需求。
6. 不要重新引入自动访客逻辑。
7. 不要迁移或重写历史路径，只为统一课程结构。
8. 不要在用户侧主流程重新引入技术叙事。
9. 不要承诺保证赚钱。
10. 不要暴露任何真实 API Key、支付密钥或数据库敏感信息。

## 15. 快速启动

```powershell
# 后端
cd backend
..\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端
cd frontend
npm run dev
```

前端默认地址：

```text
http://localhost:5173
```

后端默认地址：

```text
http://127.0.0.1:8000
```

## 16. 验证命令

```powershell
# 后端
cd backend
..\.venv\Scripts\python.exe -m pytest tests -q

# 前端
cd frontend
npm test -- --run
npm run build
```
